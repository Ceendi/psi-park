# PsiPark

Marketplace wynajmu prywatnych, ogrodzonych ogrodów dla psów na godziny (Polska, PLN).
Backend: Django 5.2 + DRF + Channels. Frontend: React 19 + Vite (osobna aplikacja).

- **Pełna specyfikacja:** [`docs/PLAN.md`](docs/PLAN.md)
- **Zasady pracy zespołowej / agentowej:** [`CLAUDE.md`](CLAUDE.md)

## Wymagania

- Docker + Docker Compose (backend uruchamiany **wyłącznie** w Dockerze — WeasyPrint wymaga bibliotek systemowych).

## Quickstart

```bash
cp .env.example .env
docker compose up --build
docker compose exec backend python manage.py seed_demo   # tworzy konto admina
```

Po starcie:

| Co | Adres |
|---|---|
| API (health) | http://localhost:8000/api/v1/health/ |
| Swagger UI (DEBUG) | http://localhost:8000/api/v1/docs/ |
| Django Admin | http://localhost:8000/admin/ |
| Mailpit (podgląd maili) | http://localhost:8025 |
| Frontend (po F0) | http://localhost:5173 |

## Usługi Compose

`db` (Postgres 17), `redis` (7), `mailpit`, `backend`. Dwie usługi są za profilami i startują dopiero, gdy będą potrzebne:

```bash
docker compose --profile frontend up   # gdy powstanie frontend/ (część F0)
docker compose --profile stripe up      # forwarder webhooków Stripe (część B5)
```

## Częste komendy

```bash
docker compose exec backend pytest                       # testy + coverage
docker compose exec backend ruff check .                 # lint
docker compose exec backend ruff format .                # formatowanie
docker compose exec backend python manage.py makemigrations
docker compose exec backend python manage.py spectacular --file schema.yaml   # regeneracja schematu OpenAPI
```

## Decyzje implementacyjne

Log świadomych wyborów wykraczających poza literalny zapis PLAN-u (uzupełniany przez kolejne części):

- **B0:** usługi `frontend` i `stripe-cli` zdefiniowane w `docker-compose.yml`, ale za profilami (`frontend`, `stripe`) — dzięki temu `docker compose up` w fazie szkieletu podnosi sam stos backendu bez błędów (brak jeszcze `frontend/` i kluczy Stripe).
- **B0:** `accounts.User` utworzony od razu w docelowym kształcie (PLAN 7.1), aby uniknąć bolesnej migracji modelu użytkownika w B1; API uwierzytelniania dostarcza B1.
- **B0:** próg pokrycia testами ustawiony na 70% jako baza szkieletu; docelowo rośnie ku 75%+ wraz z logiką w kolejnych częściach (PLAN 6.4).
- **B1:** minimalna fasada `apps/notifications/services.send(template_key, *, to, context)` utworzona już w B1 (sygnatura wg PLAN 10.2/17.3), bo B1 musi wysyłać maile `welcome` i `password_reset`, a DoD wymaga maili w Mailpit. Rejestr `TEMPLATES` zawiera tylko te dwa klucze; **część B10 rozbudowuje go o pozostałe szablony bez zmiany sygnatury** (PLAN 17.3 zakładał stub z B0 — B1 dostarcza jego minimalną, działającą wersję, by nie wysyłać maili z pominięciem fasady, PLAN 6.1).
- **B1:** link resetu hasła prowadzi na trasę frontu `${FRONTEND_BASE_URL}/reset-hasla?uid=<uid>&token=<token>` (uid = base64 PK, token = `default_token_generator`). Ostateczną ścieżkę/ekran dostarcza część F1; backend przyjmuje `uid`+`token` w `POST /auth/password/reset/confirm/`.
- **B1:** `DELETE /me/` realizuje RODO przez **anonimizację w miejscu** (scrub PII + `is_active=False` + unusable password + blacklista refresh tokenów), nie fizyczne usunięcie wiersza — bo rezerwacje/faktury (dokumenty księgowe) muszą zostać zachowane (PLAN 11).
- **B1:** w `config/settings/test.py` stawki throttlingu nie są pustym słownikiem, lecz bardzo wysokie (`100000/min`) — `ScopedRateThrottle` rzuca `ImproperlyConfigured` przy nieznanym scope, więc scope `auth` musi istnieć; test throttlingu obniża `auth` lokalnie przez `override_settings`.
- **B10:** `send()` zyskał opcjonalny argument `attachments` (dodatek wstecznie zgodny — istniejące wywołania `send(key, to=…, context=…)` działają bez zmian) wyłącznie po to, by zrealizować wymóg „faktura PDF w `reservation_paid`" (PLAN 10.2). Helper `invoice_pdf_attachment(*, number, pdf)` buduje krotkę `(filename, content, mimetype)` i jest **odsprzężony od `invoices.Invoice`** (B10 zależy tylko od B0) — B5/B6 przekazują gotowy PDF (bajty lub `FieldFile`) i numer; slashe w numerze zamieniane na `_` w nazwie pliku.
- **B10:** kontrakt kontekstu nowych szablonów: odbiorcę przekazujemy jako `user`, a dane domenowe jako `reservation` / `garden`. Pola czytane są **kaczo** (`{{ reservation.garden.title }}`), więc działają zarówno z instancjami modeli (B4/B5/B9), jak i ze słownikami (testy B10). Kwoty i daty formatuje szablon (wbudowana lokalizacja PL Django + filtr `|date`), nie każdy wołający — mniej powielania.
- **B10:** wspólna, brandowana stopka (`emails/_footer.html|txt`, dołączana przez nowe szablony) niesie dane platformy i disclaimer „platforma nie odpowiada za szkody wyrządzone pupilowi" (PLAN 11). Dwa szablony z B1 (`welcome`, `password_reset`) pozostawione bez zmian, by nie ryzykować regresji ich testów.
- **B10:** `reservation_cancelled` to jeden szablon „do drugiej strony" (przy anulowaniu przez klienta trafia do gospodarza), z opcjonalną flagą `refunded`; `reservation_rejected` i `garden_rejected` przyjmują opcjonalny `reason`. `notifications` nie dodaje endpointów ani modeli — `backend/schema.yaml` bez zmian.
- **B2:** `Dog.health_status` liczone w `dogs/services.py` (próg `HEALTH_EXPIRING_DAYS=30`), nie na modelu (PLAN 7.2). Status ogólny to „najgorszy" ze szczepień i odrobaczania (severity: `expired` > `expiring_soon` > `unknown` > `valid`). Serializer wystawia też `vaccinations_status` i `deworming_status`, by front pokolorował każdą pozycję osobno (mockup „Moi pupile") — logika progów zostaje po stronie BE, nie w szablonie.
- **B2:** `age_label` to property modelu zwracające polską etykietę wieku (np. „3 lata", „5 miesięcy"), zgodnie z PLAN 7.2; surowe `birth_date` jest również w API, więc front ma dane do własnego formatowania. Odmianę liczebników obsługuje helper `_pl_plural`.
- **B2:** Pies ma jedno pole `photo` (brak osobnej miniatury, inaczej niż `GardenPhoto`); upload przechodzi przez `core.images.process_upload` i zapisuje skompresowany obraz (≤1920 px). Błędy rozmiaru/MIME (Pillow) tłumaczone na 400 `{"photo": [...]}`.
- **B2:** Dostęp do cudzego psa zwraca **404** (nie 403) — prywatność/anty-enumeracja; własność egzekwuje selektor `owned_dogs` (zawężenie do właściciela), rolę — `IsClient`. Macierz testów: 401 (brak auth) / 403 (host) / 404 (obcy zasób).
- **B2:** Usunięcie psa z rezerwacjami blokuje **FK `Reservation.dog` PROTECT** (PLAN 7.4): `delete_dog` łapie `ProtectedError` → `DogHasReservations` (409, code `dog_has_reservations`). Rozwiązanie nie wymaga znajomości API B4 (martwe do czasu B4); kod błędu nie figuruje w tabeli 6.3 — do ewentualnego dodania do kontraktu w razie potrzeby (§6).
- **B2:** Listy/szczegóły psów używają generyków DRF (pierwszy endpoint listowy), ale zapisy idą przez `services`; `PUT` wyłączony (kontrakt zna tylko `PATCH`). Upload zdjęcia to osobny `APIView` (multipart) → poprawny, paginowany schemat (`PaginatedDogListList`).
- **B2:** `seed_demo` zyskał krok `_seed_dogs`: tworzy demo-klientkę „Katarzyna Nowak" (`katarzyna@psipark.local` / `klient12345`) jako właścicielkę psów (brak innego seeda klienta), idempotentnie (`get_or_create`), oraz Łatę i Borysa z datami zdrowia rozłożonymi na `valid/expiring_soon/expired` (pokaz badge'y). Kolejne części reużywają klientki po e-mailu.
- **B3:** Publiczny `GET /gardens/{id}/` jest **świadomy roli**: anonim/obcy widzą tylko `approved+active` (inaczej 404), właściciel widzi też własny ogród w każdym statusie (podgląd `pending`), admin — wszystko. Realizuje to selektor `visible_gardens(user)`. Publiczna **lista** pozostaje wyłącznie `approved+active`; swoje ogrody host ogląda przez `/host/gardens/`.
- **B3:** Reguła reweryfikacji (PLAN 8.2): edycja **lokalizacji** (`city/address/latitude/longitude`) na ogrodzie `approved` cofa go do `pending` i czyści `rejection_reason`; dodanie/usunięcie **zdjęcia** też (PLAN: „zmiana lokalizacji/zdjęć → pending"). Zmiana ceny/godzin/udogodnień oraz **zmiana kolejności zdjęć** (kosmetyczna) **nie** wymuszają reweryfikacji. Porównujemy realną zmianę wartości, więc PATCH tym samym miastem nie ruszy statusu.
- **B3:** Agregaty ocen (`rating_avg`/`rating_count`) są **anotowane** w selektorze (nigdy per-obiekt, PLAN 6.2). Model `reviews` powstaje w B7 — do tego czasu relacja `reviews` nie istnieje, więc selektor wykrywa to (`_reviews_relation_installed`) i degraduje do `rating_avg=None`, `rating_count=0` (sort `-rating_avg` nie wywala się). Endpoint `GET /gardens/{id}/reviews/` z tabeli 8.2 **świadomie odłożony do B7**: wymaga modelu i serializera recenzji (własność B7), a jego kształt JSON nie jest ustalony w 8.3 — wpisanie go tu oznaczałoby wymyślanie kontraktu (§6, PLAN 1.2.1).
- **B3:** `GET /gardens/{id}/availability/` liczy sloty pełnogodzinne z okna otwarcia minus kolizje. Właścicielem reguły kolizji jest `reservations` (B4, PLAN 7.4.2) — `availability` woła **leniwie** `reservations.selectors.busy_intervals(garden, date_range=(start, end))`; dopóki B4 nie istnieje, import się nie udaje i ogród jest w całości wolny. Daty liczone strefowo (`Europe/Warsaw`, `make_aware`) — poprawnie pod DST (PLAN AD-15).
- **B3:** Filtry listy (PLAN 8.2): `city` → `icontains` (działa i dla selektora miasta, i dla wpisywania); `max_dogs` → „ogród przyjmuje **co najmniej** N psów" (`max_dogs >= N`); `amenities` (CSV) → JSON `contains` (wszystkie wymagane); `in_bbox=minLng,minLat,maxLng,maxLat` → zakres współrzędnych (błędny bbox ignorowany). `time_from`/`time_to` filtrują po godzinach otwarcia; `date` jest **przyjmowana i walidowana**, ale filtrowanie kolizyjne per-ogród na liście rozsadziłoby budżet zapytań i należy do B4 — dokładną dostępność daje endpoint availability. Każdy filtr = jeden predykat SQL (budżet listy ≤6).
- **B3:** Enumy w API pozostają **angielskie** (PLAN 1.2.3): `amenities` to lista kodów (`["pool","water"]`), a w szczegółach dochodzi **read-only** `amenities_display` = `[{code,label}]` z polskimi etykietami z `Amenity.choices`. To nie łamie reguły „enum po angielsku" (wartością enuma jest kod), a daje frontowi kanoniczny katalog etykiet z backendu (anty-dryf), zamiast utrzymywać równoległą mapę.
- **B3:** `DELETE /host/gardens/{id}/` próbuje twardego usunięcia; gdy `PROTECT` (rezerwacje, B4) zablokuje — robi **miękką dezaktywację** (`is_active=False`), by zachować dokumenty księgowe (PLAN 8.2). Do czasu B4 zawsze usuwa twardo. Reguła „blokady przy przyszłych rezerwacjach" dopnie B4 (jest właścicielem rezerwacji).
- **B3:** `PATCH /host/gardens/{id}/photos/reorder/` przyjmuje `{"photo_ids":[...]}` (pełna lista id zdjęć ogrodu, pierwszy = okładka) — kształt prostszy i lepiej typowany w OpenAPI niż gołe `[...]` z 8.2; walidacja wymaga dokładnie identyfikatorów tego ogrodu.
- **B3:** `seed_demo` zyskał `_seed_gardens`: 3 gospodarzy (w tym zweryfikowana „Magda Krawczyk", hasło `gospodarz12345`) i 6 krakowskich ogrodów z realnymi współrzędnymi — 4 `approved`, 1 `pending` (kolejka admina), 1 `rejected`. **Bez zdjęć** — tak jak `_seed_dogs` (spójność + `seed_demo` nie zapisuje do realnego wolumenu media w testach seeda innych części, które nie przekierowują `MEDIA_ROOT`); zdjęcia wgrywa się przez API, a katalog/okładka obsługują ogrody bez zdjęć (`cover_image=null`).
- **B4:** **Antykolizja dwuwarstwowa (PLAN 7.4.2), ale warstwa DB obejmuje tylko statusy „zatwierdzone"** `{awaiting_host, confirmed}`. Warunek `ExclusionConstraint` (btree_gist, `tstzrange(start,end,'[)')`) musi być funkcją samego wiersza — `expires_at > now()` jest zależne od czasu i nie da się go w nim wyrazić; objęcie `pending_payment` bezwarunkowo blokowałoby slot przez wygasłe-niesprzątnięte rezerwacje, łamiąc leniwe wygasanie (AD-11). Dlatego holdy `pending_payment` egzekwuje **warstwa serwisowa**: `create_reservation` bierze `select_for_update` na ogrodzie (serializuje równoległe rezerwacje tego ogrodu), a `has_collision`/`busy_intervals` liczą pending tylko z `expires_at > now`. Slot zwalnia się więc o TTL bez sprzątania, a constraint DB to twardy backstop dla rezerwacji już opłaconych/zaakceptowanych (gdzie kolizja byłaby katastrofą). Test dowodzi obu warstw (serwis → 409 `slot_unavailable`, DB → `IntegrityError`).
- **B4:** **`busy_intervals(*, garden, date_range)` to kontrakt seam-u z B3** (PLAN 15-B3): `reservations` jest właścicielem reguły kolizji, a `gardens.availability` woła go leniwie. Zwraca okna `(start, end)` aktywnych rezerwacji (`awaiting_host`/`confirmed` + `pending_payment` z `expires_at > now`). Test integracyjny w B4 potwierdza, że realna rezerwacja zaciemnia slot w `gardens.availability`.
- **B4:** **Migracja `0001` instaluje `btree_gist`** (`BtreeGistExtension`) przed dodaniem constraintu; dodano `django.contrib.postgres` do `INSTALLED_APPS` (jedyna część używająca constraintów/typów zakresowych Postgresa) — zmiana **wyłącznie addytywna** (CLAUDE.md §3). FK `Reservation.{client,garden,dog}` są `PROTECT`, co **automatycznie aktywuje** odłożone ścieżki `ProtectedError` w `dogs.services.delete_dog` (→ `DogHasReservations` 409) i `gardens.services.delete_garden` (→ miękka dezaktywacja) — bez dotykania kodu B2/B3.
- **B4:** **Maszyna stanów (PLAN 7.4.1) w jednej bramce** `services.transition(*, reservation, target)` — pilnuje dozwolonych krawędzi (inaczej `InvalidStateTransition` 409 `reservation_state_invalid`) i stempluje znaczniki (`paid_at`+czyści `expires_at` przy `awaiting_host`; `decided_at`; `cancelled_at`). `accept`/`reject`/`cancel` obudowują ją o efekty uboczne. Decyzje hosta i anulowanie ładują rezerwację z `select_for_update` zawężoną do właściciela (obcy zasób → 404, prywatność PLAN 11).
- **B4:** **Polityka zwrotów (AD-5):** `pending_payment` → bez zwrotu (nic nie pobrano); `awaiting_host` → zwrot; `confirmed` → zwrot tylko, gdy `now ≤ start − FREE_CANCELLATION_HOURS` (inaczej host zachowuje przychód). `POST /reservations/{id}/cancel/` zwraca `{refunded: bool, reservation: {...}}`. **Refund idzie przez leniwą fasadę `payments.services.refund_if_paid`** — do czasu B5 import się nie udaje i jest to no-op (w B4 nic nie jest realnie pobierane); `reject`/`cancel` działają mimo to.
- **B4:** **Hooki maili** (kontekst wg B10: odbiorca jako `user`, domena jako `reservation`/`garden`): create → `reservation_created` do hosta; accept → `reservation_accepted` do klienta; reject → `reservation_rejected` (+`reason`) do klienta; cancel klienta → `reservation_cancelled` (+flaga `refunded`) **do hosta** (szablon „do drugiej strony"). Wysyłka synchronicznie w serwisie (jak B1), w `@transaction.atomic`.
- **B4:** **Widoczność i taby:** host (`/host/reservations/`) widzi **tylko opłacone** rezerwacje swoich ogrodów (`paid_at` ustawione) — nie widzi porzuconych, nieopłaconych prób klienta. `status_group` klienta = `upcoming`/`completed`/`cancelled`; hosta = `pending`/`accepted`/`completed`/`cancelled` (rozłączne, oparte o status + `end_time` vs teraz). Nieznany `status_group` → 400.
- **B4:** **`GET /host/stats/`**: `total_earnings` = suma `subtotal` (część gospodarza, bez prowizji) po `confirmed`; liczniki pending/upcoming/completed; `rating_avg` anotowane z recenzji ogrodów hosta, **degraduje do `None` do czasu B7** (ta sama reguła co agregaty w `gardens`). `GET /host/schedule/?from=&to=&garden=` zwraca tylko rezerwacje zajmujące kalendarz (`awaiting_host`/`confirmed`) w oknie dni włącznie.
- **B4:** **Walidacja okna** (pełne godziny zegarowe, jeden dzień, przyszłość, w godzinach otwarcia, ≥ `min_booking_hours`, `dogs_count` ≤ `max_dogs`) liczona **strefowo** (`localtime`, DST-safe, AD-15) → 400 z kluczem pola. Szczepienia psa muszą być ważne **na dzień wizyty** → `DogVaccinationRequired` (400 `dog_vaccination_required`). `expires_at = now + RESERVATION_PAYMENT_TTL_MINUTES`; komenda `cleanup_expired_reservations` (AD-11) kasuje wygasłe holdy — poprawność slotów od niej nie zależy. Eksport `GET /reservations/export.csv` z BOM-em UTF-8 (polskie znaki w Excelu).
- **B4 — świadomie poza zakresem (15-B4):** (1) kolizyjne filtrowanie `date` na **liście** ogrodów pozostaje pass-through B3 — wymaga zmiany w `gardens/filters.py` (cudza część) i nie ma go w zakresie 15-B4; dokładną dostępność daje endpoint `availability` zasilany moim `busy_intervals`. (2) Zaostrzona reguła „blokuj usunięcie ogrodu/psa, gdy istnieją **przyszłe** rezerwacje" — `PROTECT` już zachowuje historię (miękka dezaktywacja / 409), a wariant „tylko przyszłe" dotykałby plików B2/B3. (3) `reservation_paid` (mail + faktura przy `payment_intent.succeeded`) należy do B5/B6.
- **B5:** **Fasada `payments/gateway.py` to jedyny moduł importujący `stripe`** (PLAN 6.1 DIP) — funkcje modułowe (`create_payment_intent`/`refund`/`construct_event`), spójnie z resztą kodu (`services`/`selectors` też modułowe). Konwersja na grosze (`int(Decimal*100)`, AD-16) i waluta `pln` żyją **wyłącznie** tu; warstwy wyżej operują `Decimal` PLN. PaymentIntent przypięty do `payment_method_types=["card"]` — płatność **tylko kartą** (BLIK/P24/wallety usunięte przez właściciela, PLAN 16.1.1), bez redirectu. `construct_event` opakowuje błędy Stripe (`ValueError`/`SignatureVerificationError`) w `gateway.WebhookSignatureError`, żeby widok webhooka nie importował `stripe`.
- **B5:** **`start_payment` idempotentny dwuwarstwowo:** `select_for_update` na rezerwacji (serializuje równoległe żądania) + **klucz idempotencji Stripe** `pi-reservation-{id}` — ponowne wywołanie (klient wraca do formularza) zwraca **ten sam** intent i `client_secret` zamiast tworzyć duplikat; `Payment.reservation` jest `OneToOne`, więc DB też pilnuje jednej płatności na rezerwację. `client_secret` nie jest trzymany w bazie (transient, tylko do FE).
- **B5:** **Bramki stanu płatności:** start na rezerwacji nie-`pending_payment` → `PaymentAlreadyProcessed` (409, `payment_already_processed`); na **wygasłym** holdzie `pending_payment` → `ReservationExpired` (410, `reservation_expired`). Tabela 8.2 streszcza to jako „409 jeśli opłacona/wygasła", ale **kanoniczna jest tabela wyjątków 6.3** (niesie `code`, który FE rozróżnia) — `reservation_expired` = 410 GONE, więc 410 wygrywa z prozą 8.2.
- **B5:** **Webhook = źródło prawdy (AD-4, 10.1), nie front.** `payment_intent.succeeded` → płatność `succeeded` + `paid_at`, a rezerwacja przez **istniejącą bramkę** `reservations.services.transition(target=awaiting_host)` (ustawia `paid_at`, czyści `expires_at`) → faktura (B6, leniwie) → mail `reservation_paid` do klienta. **Idempotencja po `payment.status`** (PLAN 10.1: bez osobnej tabeli zdarzeń — ponowna dostawa to no-op). `payment_failed` → płatność `failed`, hold zostaje opłacalny do TTL. `charge.refunded` → `refunded` (idempotentne potwierdzenie zwrotu).
- **B5:** **`refund_if_paid(*, reservation)` to kontrakt seam-u z B4** (PLAN 17.3): `reservations.reject/cancel` woła go leniwie. Zwrot ustawia status `refunded` **od razu** (spójność DB tuż po decyzji) i woła `gateway.refund`; asynchroniczny webhook `charge.refunded` potwierdza go idempotentnie. Test integracyjny dowodzi całej ścieżki (`reject` → `gateway.refund` + płatność `refunded`).
- **B5:** **Seam faktury/maila do B6:** `_generate_invoice` woła **leniwie** `invoices.services.generate_invoice(*, reservation)` (kontrakt B6: zwraca obiekt z `.number` i `.pdf`); dopóki `apps.invoices` nie istnieje, import się nie udaje i **mail `reservation_paid` wychodzi bez załącznika** (szablon B10 ma `invoice_number` opcjonalny). Kontekst maila: `user` (klient) + `reservation` (+ `invoice_number` gdy jest). Załącznik składany helperem B10 `invoice_pdf_attachment`.
- **B5:** **Webhook bez JWT/CSRF/throttlingu:** `StripeWebhookView` ma `authentication_classes=[]` (DRF pomija SessionAuth → brak CSRF, PLAN 11), `throttle_classes=[]` (Stripe może wysłać serię), `AllowAny`; autoryzacja **wyłącznie podpisem** weryfikowanym na surowym `request.body`. Zły podpis → 400 (`code: invalid_signature` — to odpowiedź **wewnętrzna dla Stripe**, nie część kontraktu FE). Trasa `/payments/webhook/` jest stała (forwarder `stripe-cli` z compose). Endpoint `payment-intent` (`/reservations/{id}/payment-intent/`) należy do `payments/urls.py` — `reservations` świadomie zostawił ten pod-route B5; `GET /payments/config/` wystawia publiczny `publishable_key`.
- **B5:** **Serializer rozliczeń:** blok adresowy (`billing_name`/`email`/`address`/`postal_code`/`city`) **wymagany** (zasila fakturę B6, zgodnie z `Payment.html`), `billing_company`+`tax_id` (NIP) opcjonalne („faktura na firmę"), `billing_country` domyślnie `PL`. `seed_demo` zyskał `_seed_payments`: dla każdej opłaconej rezerwacji demo `Payment` (3 `succeeded` aktywne + 1 `refunded` z anulowanej), syntetyczne `pi_seed_*` (bez kontaktu ze Stripe). `payments` importuje `reservations` **leniwie** w funkcjach (17.3); model linkuje przez string `"reservations.Reservation"` — brak cyklu.
- **Frontend / Design (przed F0):** zaimportowano **handoff Claude Design** do `docs/design/` (9 prototypów HTML + `chats/` z intencjami) — to **wizualne źródło prawdy** dla części FE. `docs/PLAN.md` §16 zsynchronizowany z handoffem: tokeny §16.1 są **1:1 z designem** (primary zieleń `#2E7D32`/green-700, ciepła biel tła `#FAFFFA`, tekst `#1B1B1B`, Inter + **JetBrains Mono**, skala promieni 6–28 px + pill, 3 poziomy cieni, akcenty clay/sun, fokus Stripe `#635BFF`) — wcześniejsze wartości były placeholderami. Dodano mapowanie ekran→część (§16.1.1), buttony **pill** 4×3, oraz ustalono **płatność tylko kartą** (Stripe PaymentElement; pierwotny wybór BLIK/P24/Apple/Google Pay świadomie usunięty — decyzja właściciela z transkryptu). Do `package.json` (PLAN §4.2) dopisano `@fontsource-variable/jetbrains-mono` (addytywna zależność FE — **zaakceptowana przez właściciela**; cel: UI w 100% zgodne z Claude Design). **Bez zmian w kontrakcie API/modelu (§7/§8)** — to wyłącznie aktualizacja warstwy prezentacji FE.
- **F0:** zainicjowano aplikację FE (`frontend/`): Vite 6 + React 19 + TS `strict`, Tailwind v4 z tokenami w `@theme` (bez `tailwind.config.js`, PLAN §4.2/16.1), ESLint 9 flat config + Prettier, Vitest + Testing Library (jsdom). Skrypty: `dev/build/lint/typecheck/test/format/gen:api`. `npm run dev` pokazuje placeholder strony głównej oraz dev-only `/_ui` z pełnym design systemem; `lint/typecheck/test/build` zielone, interceptor JWT przetestowany (401→refresh→retry).
- **F0 — zależności typów:** dodano **wyłącznie** dev-owe paczki typów `@types/react`, `@types/react-dom`, `@types/node` (definicje TS dla już zatwierdzonych bibliotek React/Vite/Node — analogicznie do wpisanego wprost w §4.2 `@types/leaflet`). Żadnej nowej zależności runtime spoza PLAN §4.2.
- **F0 — tokeny:** wartości w `src/index.css` są 1:1 z `docs/design/project/Design System.html` (+ `assets/auth.css`). Dołożono tokeny miękkich tła plakietek (`--color-warning/danger/info-soft` + `-ink`) — pochodzą wprost ze stylów `.badge-*` w handoffie, więc to nie zmiana palety, tylko jej skodyfikowanie (anty-dryf zamiast wartości arbitralnych w komponencie).
- **F0 — auth/JWT (AD-3, §11):** access token wyłącznie w pamięci JS, refresh w `localStorage` (gdy „Zapamiętaj mnie") albo `sessionStorage` (gdy nie). 401 uruchamia **jedno** kolejkowane odświeżenie współdzielone przez równoległe żądania; porażka refresh → `clearTokens` + `onAuthFailure`. `setAuthFailureHandler` odsprzęga `shared/api` od Reacta (DIP). Refresh idzie przez tę samą instancję `api`, ale endpointy `auth/` są wyłączone ze ścieżki retry.
- **F0 — bundling (PLAN §12):** komponenty ciężkie/efektowne (`MapView`→leaflet, `DatePicker`→react-day-picker) **nie** są reeksportowane z barrela `shared/ui` (importuje się je bezpośrednio + lazy), żeby leaflet/kalendarz nie trafiły do głównego chunku; dostały też `export default` pod `React.lazy`. Bundle prod main ≈ 127 kB gz (cel <200 kB), bez leaflet/react-day-picker.
- **F0 — `/_ui`:** strona-demo design systemu tylko w dev (`import.meta.env.DEV`), lazy-loadowana i usuwana z builda produkcyjnego (DCE). Zastępuje Storybooka (PLAN F0).
- **F0 — routing szkieletu:** pełna tablica tras (PLAN §16.2) z layoutami (Public/Auth/3×Dashboard) i guardami `RequireRole`; ekrany części F1–F8 renderują `PlaceholderPage` (z numerem części), żeby cały routing + guardy były nawigowalne na szkielecie. Badge'y w sidebarach i widget zarobków hosta to neutralne placeholdery (bez zmyślonych liczników) — realne dane podłączą części-właściciele.
- **F0 — proxy:** cel proxy Vite (`/api`,`/ws`) konfigurowalny przez `VITE_PROXY_TARGET` (compose: `http://backend:8000`; host domyślnie `http://localhost:8000`) — dopisany jako `environment` usługi `frontend` (addytywnie, usługa należy do F0).
- **F1 — ekrany auth:** zbudowano `features/auth/` (Logowanie, Rejestracja, Reset) wg `docs/design/project/Login.html`/`Register.html` (+ `assets/auth.css`). Formularze renderują się w `AuthLayout` (split-screen + testimonial) części F0 przez `<Outlet/>` — `AuthLayout` należy do F0, F1 go **nie modyfikuje**. Komponenty z `shared/ui` (Button/Input/Checkbox/FormField) + lokalne złożenia w `features/auth/components/` (PasswordInput z podglądem, miernik siły hasła, segment Klient/Gospodarz, przyciski SSO). 4 stany widoku obsłużone; teksty PL, enumy EN.
- **F1 — dwie zgody → jedno pole `terms_accepted`:** mockup ma **dwa** wymagane checkboxy (RODO + Regulamin) i jeden opcjonalny (newsletter), a model (§7.1) ma jedno `terms_accepted_at` + `marketing_consent`. FE wymaga obu zgód (CTA „Załóż konto" zablokowane do zaznaczenia obu — jak w prototypie) i wysyła `terms_accepted: true` + `marketing_consent`. Zgoda RODO jest walidowana tylko po stronie FE (brak osobnego pola w kontrakcie).
- **F1 — telefon wymagany na FE:** prototyp oznacza pole telefonu jako `required`, więc zod wymaga go i waliduje format PL (`^(?:\+48)?\d{9}$`, spacje/myślniki usuwane — zgodnie z `accounts/validators.py`), mimo że backend dopuszcza pusty `phone`. FE może być surowszy; kontrakt nietknięty.
- **F1 — reset hasła po query paramach:** link z maila to `${FRONTEND_BASE_URL}/reset-hasla?uid=…&token=…` (ustalone i zmergowane w B1 — wiążące). Ekran `/reset-hasla` obsługuje **oba** tryby: bez paramów → „wyślij link", z `uid`+`token` → „ustaw nowe hasło" (`POST /auth/password/reset/confirm/`). Trasa `/reset-hasla/:token` z §16.2 jest zachowana i wskazuje ten sam komponent (backend jej nie emituje — czyta query).
- **F1 — błędy API:** `applyApiErrors` mapuje DRF `{pole: [msg]}` na pola RHF (`setError`), a `detail`/`non_field_errors`/nieznane pola/sieć → baner formularza. Na 401 (logowanie) pokazujemy własny PL komunikat „Nieprawidłowy e-mail lub hasło." zamiast surowego detailu simplejwt; na 429 (scope `auth` 10/min) — „Zbyt wiele prób. Odczekaj chwilę…".
- **F1 — SSO i warstwa danych:** przyciski Google/Apple są **wyłącznie wizualne** (§18) — wyglądają jak w prototypie (nie wyszarzone), klik pokazuje toast „już wkrótce" + `title`. Logowanie/rejestracja delegują do `AuthContext` (właściciel tokenów/stanu) opakowanego w mutacje React Query w `features/auth/api.ts`; endpointy resetu (bezstanowe) wołają `api` bezpośrednio w warstwie danych. Ikony pól generyczne z `lucide-react`, tematyczne (łapa/ogród) z `shared/ui/icons`. Po sukcesie redirect wg roli: klient→`/panel`, host→`/gospodarz` (logowanie honoruje bezpieczny `?next=`).
- **B6 — numeracja faktur:** numer `PSI/RRRR/MM/NNNN` przydzielany z tabeli-licznika `InvoiceSequence` (jeden wiersz na `(rok, miesiąc)`, `UniqueConstraint`) blokowanej `select_for_update` w `services._next_number` — równoległe wystawienia serializują się i nigdy nie dzielą numeru (PLAN 7.6). Rok/miesiąc liczone z `timezone.localtime` (Europe/Warsaw, AD-15), więc numeracja resetuje się 1. dnia miesiąca lokalnie. `Invoice.reservation` jest `OneToOne(PROTECT)` — faktura to niezmienny dokument księgowy (w Django Admin read-only, bez add/delete), chroni ją też RODO-anonimizacja z B1 (§11).
- **B6 — `generate_invoice` idempotentny + seam B5:** `services.generate_invoice(*, reservation)` zwraca istniejącą fakturę, gdy już jest (ponowna dostawa webhooka `payment_intent.succeeded` to no-op), inaczej przydziela numer, renderuje PDF i zapisuje — całość w `@transaction.atomic`, więc nieudany render nie „spala" numeru. B5 woła to **leniwie** (`payments.services._generate_invoice`, try/except ImportError) — teraz, gdy `apps.invoices` istnieje, mail `reservation_paid` realnie niesie PDF (helper B10 `invoice_pdf_attachment`). Z tego powodu zaktualizowano **jeden test B5** (`test_webhook_succeeded_..._sends_plain_mail` → `..._attaches_invoice_pdf`): wcześniej dokumentował zachowanie „B6 nieobecne", teraz weryfikuje załącznik (zmiana wymuszona przez seam, nie kosmetyka).
- **B6 — szablon PDF (WeasyPrint):** `from weasyprint import HTML` importowany **leniwie** w `_render_pdf` (ciężkie biblioteki natywne Pango są tylko w obrazie Dockera, PLAN 4.1 — import modułu nie wymaga ich na gołym hoście). Szablon `templates/invoices/invoice.html` (PL): sprzedawca = `settings.INVOICE_SELLER` (dict-stała, nie env — to dane platformy, nie sekret), nabywca = blok rozliczeniowy z `Payment` (z fallbackiem na dane konta klienta, gdy brak), pozycje: wynajem (`subtotal`) + prowizja serwisowa (`service_fee`), suma brutto = `total_gross`. Model trzyma **tylko** kwotę brutto, więc faktura nie wymyśla rozbicia VAT — adnotacja „sprzedawca zwolniony z VAT, kwoty brutto". Kwoty formatowane po polsku (`89,50 zł`, NBSP jako separator) w serwisie (deterministycznie, bez zależności od lokalizacji Django); daty przez filtr `|date`.
- **B6 — endpointy tylko dla właściciela:** `GET /reservations/{id}/invoice/` (metadane) i `/invoice/pdf/` (pobranie) mają `IsClient` + selektor `owned_invoice` zawężony do klienta. Obcy klient, nieistniejąca rezerwacja **oraz** rezerwacja jeszcze nieopłacona (brak faktury) → jednakowo **404** (anty-enumeracja, PLAN 11; „brak faktury przed płatnością" = 404); host (zła rola) → **403**. PDF serwowany `FileResponse(as_attachment=True)` (`Content-Disposition: attachment`), **nigdy** jako publiczny URL media; nazwa pliku `faktura-PSI_2026_06_0001.pdf` (slashe→`_`, jak w helperze B10). `pdf_url` w metadanych wskazuje endpoint API (uwierzytelniony), nie ścieżkę media.
- **B6 — seed:** `seed_demo` zyskał `_seed_invoices`: dla każdej płatności `succeeded` (3 aktywne opłacone rezerwacje demo) generuje realny PDF do `MEDIA_ROOT/invoices/` (PLAN 14 „1 opłacona rezerwacja z fakturą"), idempotentnie. Refundowana rezerwacja faktury nie dostaje (status płatności `refunded`).
- **B6 — infra (dotyka B0, zgłoszone):** (1) dodano **root `backend/conftest.py`** z autouse `_tmp_media` (przekierowanie `MEDIA_ROOT` na katalog tymczasowy) — bo `seed_demo` zapisuje teraz PDF-y, a testy seeda z innych części (payments/core/dogs) wołają `seed_demo` bez własnego przekierowania; istniejące app-owe `_tmp_media` (gardens/reservations) **nadpisują** je lokalnie (ta sama nazwa fixture), więc per test działa dokładnie jeden. (2) `backend/Dockerfile` tworzy `/app/media` jako `appuser` przed `USER appuser`, aby nazwany wolumen `media` inicjalizował się **zapisywalny dla użytkownika nie-root** — to naprawia istniejącą lukę uprawnień wolumenu media (dotyczyła też uploadu zdjęć B2/B3, ale ujawnił ją dopiero seed faktur). Na istniejących środowiskach wymaga jednorazowego `docker volume rm psi-park_media` przed `docker compose up --build`.
- **B7 — `GET /gardens/{id}/reviews/` należy do B7:** trasa figuruje w §8.2 pod „Ogrody", ale B3 świadomie ją odłożył (wymaga modelu+serializera recenzji, a kształt JSON nieustalony w §8.3). Wpięta w `apps/reviews/urls.py` (ścieżka pod `/gardens/{id}/…`, ale własność i kod = B7). Zwraca recenzje danego `garden_id` (najnowsze pierwsze, paginowane, **publicznie**); nieznany ogród → pusta strona (200), bez sprzęgania z regułą widoczności ogrodów (recenzje to dane publiczne przy publicznym zasobie).
- **B7 — agregaty ocen bez denormalizacji (zero zmian w cudzych plikach):** model `Review.garden = FK(related_name="reviews")` + `rating` **automatycznie zapala** `rating_avg`/`rating_count` anotowane już w `gardens.selectors` i `reservations.host_stats` (wykrywają relację przez `_reviews_relation_installed`). Istniejące testy B3/B4 asertujące `rating_avg=None` **nadal przechodzą bez zmian** — `AVG` z zera wierszy to `NULL`, a `Count` to `0`, więc ogród bez recenzji degraduje tak samo, czy relacja istnieje, czy nie. Żaden plik B3/B4 nie był ruszany.
- **B7 — uprawnienie do recenzji (PLAN 7.7):** `create_review` ładuje rezerwację `select_for_update` zawężoną do klienta (obca/nieistniejąca → 404, §11), wymaga stanu zakończonego (`confirmed` + `end_time < now`, inaczej `ReviewNotEligible` 403), a istnienie recenzji sprawdza po parze **(author, garden)** (inaczej `ReviewAlreadyExists` 409). Sprawdzenie po (author, garden) obejmuje i ponowną recenzję tej samej rezerwacji (OneToOne), i drugą rezerwację w tym samym ogrodzie — spójnie z `UniqueConstraint(author, garden)` (K-1, twardy backstop DB; `CheckConstraint` 1–5 jako drugi). `GET /reviews/eligible/` wyklucza ogrody już zrecenzowane, więc każdy wiersz feedu jest faktycznie do oceny.
- **B7 — jeden widok, dwa tryby + reuse:** `ReviewDetailView` ma **publiczny GET** i **właścicielski PATCH/DELETE** — `get_permissions`/`get_queryset` rozgałęziają po metodzie (GET → `review_detail_qs`; zapis → `owned_reviews(author=user)`, obca recenzja → 404). `PUT` wyłączony (kontrakt zna tylko PATCH). Karta „do wystawienia" (`EligibleReservationSerializer`) reużywa publicznego `reservations.serializers.GardenMiniSerializer` zamiast duplikować logikę okładki. FK wg PLAN 7.7 to **CASCADE** (usunięcie rezerwacji/ogrodu kasuje recenzję; RODO-anonimizacja B1 nie usuwa wierszy `User`, więc recenzje przetrwają). `seed_demo._seed_reviews` wystawia 5★ dla każdej zakończonej rezerwacji demo (idempotentnie, cap 1 ogród = 1 recenzja) → podnosi ocenę ogrodu w katalogu (smoke §20 krok 7).
- **B8 — wiadomości tylko przez WebSocket, REST = historia/stan:** kontrakt §8.2 czatu (lista/start/historia/read) **nie** zawiera „POST wiadomości" — wysyłka to wyłącznie ramka `message.send` na WS (§9), a REST serwuje historię i potwierdzenie przeczytania. Dlatego nie ma serializera zapisu wiadomości; `chat/services.post_message` woła **tylko** consumer (przez `database_sync_to_async`), kolejność persist→broadcast (§9.3). Uczestnik = `conversation.client` **lub** `garden.host` (host wynika z ogrodu, AD-10/K-4).
- **B8 — `?before=` to id wiadomości (keyset), nie znacznik czasu:** §8.2 mówi „paginacja kursorowa po czasie", ale kursorem jest **id** (autoinkrement = monotoniczny z czasem, lecz odporny na kolizje mikrosekund). `GET /conversations/{id}/messages/?before=<id>` zwraca `{results, has_more, next_before}` — strona `MESSAGE_PAGE_SIZE=30` **od najnowszych**, `next_before` = id najstarszej w stronie (do doładowania starszych na scroll-up); zły `before` (nie-liczba) → 400 `{"before": [...]}`. Kształt odpowiedzi nieustalony w §8.3 — wybrano minimalny, otypowany w OpenAPI (`MessageHistorySerializer`).
- **B8 — wiersz listy konwersacji bez N+1 (budżet §12):** `selectors.participant_conversations` liczy `unread_count` (wiadomości drugiej strony bez `read_at`) i podgląd ostatniej wiadomości **korelowanymi `Subquery`** (nie `JOIN`+`GROUP BY`), więc cała lista to jedno zapytanie + prefetch okładek (test budżetu ≤6). Wiersz niesie **obu** uczestników (`client`+`host`) i kartę ogrodu — front zna swoją rolę i sam wybiera rozmówcę (zamiast zmyślonego pola „counterparty"). `POST /conversations/` jest idempotentny (`UniqueConstraint(garden, client)`): nowy wątek → 201, istniejący → 200; tylko ogród `approved+active` można zagadać (inaczej 404).
- **B8 — autoryzacja WS przez query string + `JWTAuthMiddleware` (§9.1):** przeglądarka nie ustawi nagłówka `Authorization` na handshake'u WS, więc access JWT idzie w `?token=` i jest walidowany tą samą maszynerią simplejwt co REST; `scope["user"]` → `AnonymousUser` gdy token brak/zły/wygasły/konto zablokowane. Decyzję o uczestnictwie podejmuje **consumer**: brak auth → close `4401`, nie-uczestnik/nieistniejąca konwersacja → close `4403` (kody aplikacyjne 4000–4999). Test-only: komunikatory łączą się z `JWTAuthMiddleware(URLRouter(...))` **bez** `AllowedHostsOriginValidator` (walidator origin sprawdza realny stos `config.asgi`, nie własność B8).
- **B8 — ramki rozszerzone ponad §9.2 (badge + błędy):** §9.3 wymaga powiadomienia listy drugiej strony „grupą `user_{id}`", ale nie podaje ramki — wybrano `conversation.updated {conversation, last_message_at}` wysyłaną do grupy `user_{recipient}` (każdy zalogowany socket dołącza do `chat_{id}` **i** `user_{self}`, więc badge dociera, gdy odbiorca jest na innym wątku). Dodano też serwerową ramkę `error {code, detail}` dla walidacji treści (`content_empty`/`content_too_long`, 1–2000 znaków) i throttlingu (`rate_limited`, sliding window ≤`THROTTLE_MAX=5`/2 s, §9.3) oraz `unknown_type` — to **rozszerzenia** kontraktu §9.2, nie zmiana istniejących ramek; udokumentowane dla F7.
- **B8 — testy WS bez `pytest-asyncio` (spoza §4.1):** każdy scenariusz to `async def` uruchamiane przez `asgiref.async_to_sync`, a testy są `@pytest.mark.django_db(transaction=True)` — consumer pisze do DB w wątku roboczym (`database_sync_to_async`), więc wiersze muszą być **zacommitowane**, by były tam widoczne. Testy używają `InMemoryChannelLayer` (`config/settings/test.py` z B0), a pełny stos przez **realny Redis** (origin validator + JWT middleware + `channels_redis`) zweryfikowano ręcznym smoke-testem (dwóch uczestników wymienia wiadomość, nie-uczestnik odrzucony 4403). `config/asgi.py` (placeholder z B0) podpięto do `chat.routing` + `JWTAuthMiddleware`; wpisy w `INSTALLED_APPS`/`config/urls.py` wyłącznie addytywne (CLAUDE.md §3).
- **B9 — akcje Django Admin bez dotykania cudzych `admin.py` (CLAUDE.md §2):** modele są już zarejestrowane przez swoje części (`accounts`/`gardens`/`reviews` mają `list_display`/`search_fields`/`list_filter`), więc B9 **nie edytuje** ich `admin.py`. Zamiast tego `adminpanel/admin.py` (appka ładowana **ostatnia** w `INSTALLED_APPS`) **wyrejestrowuje i ponownie rejestruje podklasy** `GardenAdmin`/`UserAdmin`, dziedzicząc całą konfigurację i dokładając wyłącznie akcje moderacyjne (`approve/reject`, `verify/block/unblock`). Jawny `from apps.gardens.admin import GardenAdmin` gwarantuje, że model jest zarejestrowany **zanim** go wyrejestruję (niezależnie od kolejności autodiscovera). Recenzje nie dostają własnej akcji — wbudowane „usuń zaznaczone" to narzędzie moderacji. Akcje wołają `adminpanel.services` (jeden tor logiki z REST, AD-12).
- **B9 — serwisy jedynym torem logiki, reuse selektorów (zero duplikacji):** widoki i akcje adminowe wołają `adminpanel.services` (keyword-only, `@transaction.atomic`, mail w środku jak w B4). Listy reużywają warstwy odczytu właścicieli: kolejka ogrodów = `gardens.selectors.visible_gardens(user=admin)` (admin widzi wszystko, te same anotacje host+okładka+oceny, bez N+1) posortowane `created_at` rosnąco (**FIFO kolejki weryfikacji**); moderacja recenzji = `reviews.selectors.review_detail_qs()`. Dzięki temu B9 nie duplikuje querysetów ani nie denormalizuje (PLAN 12). Filtry: `?status=` (pending/approved/rejected), `role`/`is_active`, `garden`/`rating` + `search` (django-filter + `SearchFilter`).
- **B9 — kontrakt API i format błędów:** `approve` → `approved` + czyści `rejection_reason` + mail `garden_approved`; `reject` wymaga **niepustego** `reason` (surowiej niż opcjonalny `reason` w `reservations.reject` — odrzucony ogród bez wskazówki jest bezużyteczny dla hosta), zapis `rejection_reason` + mail `garden_rejected`. `verify` jest **idempotentne** (zweryfikowany host zachowuje pierwotny `verified_at`, bez drugiego maila); `block`/`unblock` idempotentne na `is_active`. Dwie bramki to **wyjątki biznesowe adminpanel-specyficzne** (precedens B2 `dog_has_reservations`, kody poza tabelą 6.3, udokumentowane): `host_role_required` (400 — weryfikować można tylko hosta) i `cannot_block_admin` (400 — ochrona przed zablokowaniem platformy samej sobie). DoD „block → login niemożliwy" potwierdzony testem przez realny endpoint `auth/login` (simplejwt odrzuca `is_active=False` → 401). Dostęp wyłącznie `IsAdmin` (inni → 403, anon → 401); brak zasobu → 404.
- **B9 — `adminpanel` bez modeli/migracji + zależność od B7:** appka to wyłącznie warstwa API/admin nad cudzymi modelami (brak `models.py`/migracji — `makemigrations --check` czyste). PLAN podaje zależności B9 = B3+B1, ale „moderacja recenzji" wymaga B7 (zmergowane, więc dostępne) — `adminpanel` importuje publiczne `reviews.selectors`/`reviews.models`. **`ENUM_NAME_OVERRIDES` w `SPECTACULAR_SETTINGS`** (`RoleEnum` → `apps.accounts.models.User.Role`) dodane addytywnie do współdzielonego `config/settings/base.py` (CLAUDE.md §3): nowe serializery admina współdzielą pełny enum `role`, co bez override'u zmusiłoby spectacular do nazwy z hashem (`RoleD6dEnum`) — pin daje **deterministyczny, czysty** schemat (regeneracja bit-w-bit identyczna, zero ostrzeżeń) i ładne typy FE. Bez zmian w modelu/kontrakcie (§7/§8) — endpointy `/api/v1/admin/…` są dokładnie z tabeli §8.2.
- **F8 — panel admina (poza handoffem, trzymany design system):** `features/admin/` z trzema ekranami pod `RequireRole admin` (guard z F0): **Kolejka weryfikacji** (`/admin`) — `GET /admin/gardens/?status=` zwraca pełny `AdminGarden` (zdjęcia, kontakt hosta, lat/lng), więc karta kolejki renderuje podgląd (galeria + parametry + udogodnienia + zasady + **mini-mapa Leaflet** + dane gospodarza) bez dodatkowego fetcha; akcje Zatwierdź / Odrzuć (modal z **wymaganym** powodem, zgodnie z B9). **Użytkownicy** (`/admin/uzytkownicy`) — tabela z filtrami rola/status + szukajka, akcje Zweryfikuj (tylko niezweryfikowany host) / Zablokuj (modal potwierdzenia) / Odblokuj; przycisk blokady ukryty dla adminów (backend i tak broni `cannot_block_admin`). **Moderacja recenzji** (`/admin/recenzje`) — lista z filtrem oceny + szukajką, usuwanie z potwierdzeniem. 4 stany widoku; dane przez hooki React Query (zero axiosa w komponentach).
- **F8 — regeneracja `schema.d.ts` + rename `UserRoleEnum`→`RoleEnum`:** `npm run gen:api` (wymagane dla typów `Admin*`) odtworzyło `schema.d.ts` z aktualnego `schema.yaml` (po B6–B9); B9 wprowadził override `RoleEnum`, więc alias `UserRole` w `shared/api/types.ts` zaktualizowano na `Schemas['RoleEnum']` (jedyna wymuszona zmiana bazowego aliasu; plik generowany nie edytowany ręcznie — CLAUDE.md §5). Mini-mapa importuje `MapView` wprost (nie z barrela), więc leaflet ląduje w leniwym chunku tras `admin`, nie w głównym bundlu (§12).
- **F8 — strony statyczne (`features/static/`):** Regulamin, Polityka prywatności (RODO), Centrum pomocy/FAQ, „Świadczenie usług drogą elektroniczną" jako jeden komponent `StaticPage` renderujący dokument z mapy treści (`content.ts`) — **placeholder prawniczy** z obowiązkowym disclaimerem o braku odpowiedzialności za szkody pupila (PLAN §11). Renderowane w `PublicLayout`; linkowane już ze **stopki F0** i z rejestracji (F1) — stopki nie ruszam (własność F0). `getApiMessage` zduplikowane lokalnie w `features/admin/errors.ts` (helper z F5 `features/account/forms` jest na niezmergowanej gałęzi F5 — brak zależności F8→F5).
